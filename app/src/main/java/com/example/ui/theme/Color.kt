package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// ==========================================
// DARK RED BRAND PALETTE (ReplyFloat AI)
// ==========================================
val RedPrimary = Color(0xFF7A0F16)          // Deep dark red
val RedPrimaryDark = Color(0xFF52090E)      // Deepest dark red
val RedPrimaryAccent = Color(0xFFA51D26)    // Primary accent red
val RedPrimaryBright = Color(0xFFC72834)    // Highlight vibrant red
val RedGlow = Color(0x33A51D26)             // Subtle glow accent

// Dark Red Tinted Surfaces & Backgrounds
val RedCanvasDark = Color(0xFF12090A)       // Background: very dark red-tinted black
val RedSurfaceDark = Color(0xFF1D0C0E)      // Surface: dark red surface
val RedSurfaceElevated = Color(0xFF281012)  // Elevated surface / container
val RedSurfaceVariant = Color(0xFF361418)   // Variant surface
val RedCardBorder = Color(0xFF4C1A1F)       // Card outline
val RedCardBorderGlow = Color(0xFFA51D26)   // Border glow
val RedGlassSurface = Color(0xF21D0C0E)     // Glass floating surface (95% opacity)

// High Contrast Text Tokens
val TextPrimary = Color(0xFFF9EBEB)         // Near-white with warm hint
val TextSecondary = Color(0xFFD4B2B6)       // Muted warm gray
val TextMuted = Color(0xFF987277)           // Subtle muted warm gray
val TextDisabled = Color(0xFF6B4B4F)        // Disabled text

// Accent & Status
val StatusEmerald = Color(0xFF2ECC71)
val StatusAmber = Color(0xFFF59E0B)
val StatusRose = Color(0xFFE74C3C)

// Tone specific tag colors (Harmonized with dark-red identity)
val ToneCasual = Color(0xFFC0392B)
val ToneProfessional = Color(0xFF8B1E28)
val ToneConcise = Color(0xFFE65100)
val ToneFriendly = Color(0xFFF57C00)
val ToneWitty = Color(0xFFD81B60)

// Pass-through state colors
val PassThroughActiveBg = Color(0x33F59E0B)
val PassThroughActiveBorder = Color(0xFFF59E0B)
val PassThroughInactiveBg = Color(0x1F6B4B4F)

// Direct alias mappings for consistent application-wide dark-red theming
val CanvasDark = RedCanvasDark
val SurfaceDark = RedSurfaceDark
val SurfaceVariantDark = RedSurfaceVariant
val SurfaceContainerDark = RedSurfaceElevated
val CardBorderDark = RedCardBorder
val CardBorderGlow = RedCardBorderGlow
val GlassSurface = RedGlassSurface
val Cyan400 = RedPrimaryAccent
val Cyan500 = RedPrimary
val Cyan300 = RedPrimaryBright
val CyanGlow = RedGlow
val Violet400 = RedPrimaryAccent
val Violet500 = RedPrimary
val Violet600 = RedPrimaryDark
val VioletDark = RedPrimaryDark
val Emerald400 = StatusEmerald
val Emerald500 = StatusEmerald
val Amber400 = StatusAmber
val Amber500 = StatusAmber
val Rose400 = StatusRose
val Rose500 = StatusRose
